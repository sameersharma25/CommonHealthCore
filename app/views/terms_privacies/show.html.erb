<p id="notice"><%= notice %></p>

  <div class="col-sm-8">
    <%= cktext_area_tag 'body', @terms_privacy.body, ckeditor: {toolbar: 'mini'}, readOnly: true %>
  </div>

<%= link_to 'Edit', edit_terms_privacy_path(@terms_privacy) %> |
<%= link_to 'Back', terms_privacies_path %>
