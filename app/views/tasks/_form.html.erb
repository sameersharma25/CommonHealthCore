<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= form.label :task_type %>
      <%= form.text_field :task_type, id: :task_type, placeholder: "Task Type" %>
    </div>

    <div >
      <%= form.label :task_status %>
      <%= form.select(:task_status, options_from_collection_for_select(@statuses, 'status', 'status'))  %>
    </div>

    <div class="field">
      <%= form.label :task_deadline %>
      <%= datetime_field_tag :task_deadline %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
