<%= form_with(model: client_application, local: true) do |form| %>
  <% if client_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_application.errors.count, "error") %> prohibited this client_application from being saved:</h2>

      <ul>
      <% client_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :client_name %>
  </div>

  <div class="field">
    <%= form.label :application_url %>
    <%= form.text_field :application_url, id: :application_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
